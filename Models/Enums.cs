namespace BorrowITEquip.Models
{
    public enum Role
    {
        Student = 0,
        Professor = 1
    }

    public enum EquipmentType
    {
        Laptop = 0,
        Phone = 1,
        Tablet = 2,
        Another = 3
    }

    public enum RequestStatus
    {
        Pending = 0,
        Accepted = 1,
        Denied = 2
    }
}
